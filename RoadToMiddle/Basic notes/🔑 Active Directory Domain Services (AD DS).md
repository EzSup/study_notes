## 1. Основна Ідея та Призначення 💡

**Active Directory (AD)** — це служба каталогів (Directory Service) від Microsoft, яка є основою корпоративної мережі Windows. Вона надає централізований, ієрархічний спосіб управління всіма мережевими об'єктами.

### 🎯 Ключові Функції:

* **Централізоване Управління Ідентичністю (Identity Management):** Створення та управління обліковими записами користувачів, груп та комп'ютерів.
* **Автентифікація та Авторизація:** Перевірка особи користувача та визначення його прав доступу до ресурсів (файли, SQL, веб-застосунки).
* **Служба Каталогів:** Єдине місце для зберігання інформації про всі об'єкти в мережі.

---

## 2. Ключові Компоненти та Ієрархія 🌳

| Термін | Опис | Роль у Мережі |
| :--- | :--- | :--- |
| **Об'єкт (Object)** | Базова одиниця AD: користувач, комп'ютер, група, принтер. | Має унікальні властивості (наприклад, ім'я, пароль, електронна пошта). |
| **Домен (Domain)** | Логічна група об'єктів (наприклад, `corp.local`). | Базовий адміністративний і безпековий периметр. |
| **Дерево Доменів (Tree)** | Група доменів, що використовують спільний простір імен та спільний каталог. | Наприклад, `eu.corp.local` та `us.corp.local`. |
| **Ліс (Forest)** | Група незалежних дерев доменів, які мають спільну схему (Schema) та глобальний каталог. | Найвищий рівень ієрархії AD. Забезпечує довіру (Trust) між усіма доменами. |
| **Контролер Домену (Domain Controller, DC)** | Сервер (Windows Server), на якому зберігається копія бази даних AD. | Обробляє запити на вхід (логін/пароль) та автентифікацію. |

---

## 3. Протоколи та Автентифікація 🔐

| Протокол | Призначення | Роль у Безпеці |
| :--- | :--- | :--- |
| **Kerberos** | Протокол автентифікації за замовчуванням. | Використовує "квитки" (Tickets) замість повторної передачі паролів, забезпечуючи **Single Sign-On (SSO)**. |
| **LDAP (Lightweight Directory Access Protocol)** | Протокол для запитів і модифікації інформації в каталозі AD. | .NET-застосунки використовують LDAP для **пошуку користувачів** та перевірки членства у групах. |
| **DNS (Domain Name System)** | Критично важливий для роботи AD. | AD використовує DNS для **локалізації** контролерів доменів та інших служб. |

---

## 4. Групові Політики (GPO) та Практика для Розробника 🛠️

**GPO (Group Policy Objects)** — це набір правил, які автоматично застосовуються до об'єктів (користувачів або комп'ютерів) у домені.

### 🎯 Взаємодія GPO та .NET-Застосунків:

1.  **Контроль Безпеки (Hardening):** GPO часто використовується для примусового вмикання **TLS 1.2+** та вимкнення старих протоколів. Це безпосередньо впливає на **HttpClient** та інші мережеві підключення у вашому .NET-додатку.
2.  **Управління Правами Доступу:** Розробник повинен надавати права доступу до файлових ресурсів, SQL Server або IIS-сайтів **не окремим користувачам**, а **AD-групам**, які керуються через GPO.
3.  **Сервісні Облікові Записи (Service Accounts):**
    * **Визначення:** Спеціальні облікові записи в AD, під якими працюють Application Pools IIS, служби Windows або планувальники завдань.
    * **Практика:** Використовуйте **Managed Service Accounts (MSA)** або **Group Managed Service Accounts (gMSA)** для автоматичного управління паролями та підвищеної безпеки.

---

## 5. Коротке Резюме 🚀

**Active Directory** забезпечує **Single Sign-On (SSO)** та **централізований контроль** над усією корпоративною інфраструктурою. Для **.NET Developer** розуміння AD є критичним для:

* Налаштування **Windows Authentication** у веб-додатках.
* Правильного використання **Service Accounts** для IIS/служб.
* Дотримання політик безпеки (TLS, права доступу), які нав'язуються через **GPO**.