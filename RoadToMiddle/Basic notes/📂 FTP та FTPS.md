## 1. Основна Ідея та Еволюція 💡

**FTP (File Transfer Protocol)** — мережевий протокол для передачі файлів між клієнтом і сервером. Це класичний, але **НЕ захищений** протокол.

**FTPS (FTP over TLS/SSL)** — це захищена версія FTP, яка використовує протоколи **TLS/SSL** для шифрування каналу зв'язку.

> ⚠️ **Ключовий Висновок:** **FTP** передає облікові дані та дані **у відкритому тексті**. **FTPS** є обов'язковим стандартом для будь-якого корпоративного деплою або обміну даними.

***

## 2. Як Працює Протокол та Безпека 🛡️

### 2.1. Подвійний Канал та Порти

FTP/FTPS використовує два окремі TCP-з'єднання:

| Канал | Порт (Стандарт) | Призначення |
| :--- | :--- | :--- |
| **Управляючий (Control)** | **TCP 21** | Обмін командами (логін, пароль, команди `GET`/`PUT`). |
| **Даних (Data)** | TCP 20 або Динамічний | Власне передача файлів та лістингів директорій. |

### 2.2. Режими Передачі (Важливо для Firewall)

Вибір режиму визначає, яка сторона ініціює з'єднання по **каналу даних**.

| Режим | Ініціатор Data-каналу | Дружність до Firewall |
| :--- | :--- | :--- |
| **Активний (Active)** | **Сервер** підключається до клієнта на високому порту. | **Погано.** Вимагає відкриття високого порту на **клієнтському** боці (рідко дозволено). |
| **Пасивний (Passive)** | **Клієнт** підключається до сервера на динамічному порту. | **Краще.** Вимагає відкриття діапазону портів лише на **серверному** боці (легше налаштувати у **Windows Firewall**). |

> **Практична Порада:** У корпоративному середовищі **завжди використовуйте Пасивний режим**.

### 2.3. Механізми FTPS (Два типи)

FTPS захищає як канал управління, так і канал даних, використовуючи TLS.

| Тип | Назва | Опис | Статус |
| :--- | :--- | :--- | :--- |
| **Implicit FTPS** | Неявний | З'єднання починається з TLS одразу на іншому порту (наприклад, 990). | **Застарілий.** |
| **Explicit FTPS** | **Явний (AUTH TLS)** | З'єднання починається по порту 21 як звичайне FTP, але потім клієнт надсилає команду **`AUTH TLS`**, щоб підвищити рівень до захищеного. | **Сучасний Стандарт.** |

***

## 3. Типові Сценарії у Microsoft-Екосистемі (.NET/IIS)

* **Деплой Веб-Додатків:** Використання FTPS для швидкої передачі бінарних файлів та статичного контенту на **IIS-сервер**. Хоча **Web Deploy** є кращим для ASP.NET-додатків, FTPS все ще використовується як простий спосіб деплою з CI/CD або вручну.
* **Інтеграція з IIS Manager:** FTP-сайти повністю налаштовуються та управляються через **IIS Manager**. Це включає:
    * Прив'язка **TLS-сертифіката** для FTPS.
    * Встановлення фізичних шляхів та налаштування **IP-обмежень**.
    * Налаштування **користувацької автентифікації** (стандартна, анонімна, або через **Active Directory**).
* **Автоматизований Обмін Даними:** Передача великих файлів, логів, звітів або бекапів баз даних на зовнішні або архівні сервери.

***

## 4. Безпека та Критичні Поради для .NET Developer 🛡️

1.  **Виключіть FTP:** Повністю вимкніть підтримку незашифрованого FTP на сервері (порт 21) та намагайтеся **не використовувати його у конфігураціях**.
2.  **SFTP vs FTPS:** Хоча FTPS є стандартом, **SFTP (SSH File Transfer Protocol)** часто вважається більш сучасним та простим для налаштування (працює лише через один порт, зазвичай 22). Обирайте SFTP, якщо інфраструктура його підтримує.
3.  **Ізоляція Користувачів:** У налаштуваннях FTP-сайту IIS використовуйте **User Isolation** (ізоляцію користувачів), щоб гарантувати, що кожен користувач може бачити лише свій домашній каталог (Home Directory), запобігаючи перегляду файлів інших користувачів або системних папок.
4.  **Спеціалізовані Облікові Записи:** Створюйте окремі, неінтерактивні **Service Accounts** (облікові записи служб) в Active Directory спеціально для деплою через FTPS, надаючи їм мінімально необхідні права лише на папку застосунку.
5.  **Логування (Logging):** Завжди вмикайте детальне логування (IIS/FTP Logging), щоб відстежувати, хто, коли і які файли передавав, для цілей аудиту та безпеки.