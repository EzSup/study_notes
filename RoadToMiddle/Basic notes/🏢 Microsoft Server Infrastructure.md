## 1. Основна Ідея та Призначення 💡

**Microsoft Server Infrastructure** — це інтегрована екосистема технологій, необхідна для побудови корпоративних мереж, хостингу застосунків та централізованого управління ресурсами. Вона забезпечує основу для всіх великих бізнес-проєктів на базі Windows.

**Ключові компоненти:** **Windows Server**, **Active Directory (AD)**, набір ролей (IIS, RDS) та мережеві служби.

***

## 2. Центральні Служби та Ролі (Windows Server)

### 2.1. Active Directory Domain Services (AD DS) 🔑

Це ядро інфраструктури, що забезпечує централізоване управління, автентифікацію та авторизацію.

| Компонент | Призначення | Деталі для Розробника |
| :--- | :--- | :--- |
| **Домен (Domain)** | Логічна група комп'ютерів і користувачів. | Ваш застосунок може використовувати **доменну автентифікацію (Windows Authentication)**. |
| **Користувачі & Групи** | Облікові записи та логічні об'єднання користувачів. | Права доступу до файлової системи, SQL Server, або IIS-сайтів надаються **через AD-групи**. |
| **GPO (Group Policy Objects)** | Централізоване управління конфігурацією клієнтів і серверів. | Регулює налаштування безпеки (наприклад, версії TLS), що може вплинути на **деплой** або **роботу застосунку**. |
| **Автентифікація** | Використовує протокол **Kerberos** для безпечного входу в домен. | Важливо для **Service Accounts**, які використовуються Application Pools в IIS. |

### 2.2. Веб-Хостинг: IIS (Internet Information Services) 🌐

Сервер для хостингу веб-додатків (ASP.NET, .NET Core, статичні сайти) та API.

| Термін IIS | Призначення | Роль у Деплої .NET |
| :--- | :--- | :--- |
| **Application Pool** | Ізольований процес-контейнер, в якому працює застосунок. | Визначає **Identity** (обліковий запис), під яким виконується код. **Service Account** має бути налаштований у AD. |
| **Virtual Directory** | Псевдонім (alias), який вказує на фізичну папку на сервері. | Використовується для організації структури сайту (наприклад, `/api` $\rightarrow$ `C:\Apps\Api`). |
| **Web Deploy** | Механізм публікації (деплою) з Visual Studio або CI/CD. | Забезпечує безпечну та інкрементальну синхронізацію файлів застосунку. |
| **HTTPS Binding** | Прив'язка **Сертифіката TLS/SSL** до сайту на порті 443. | Необхідно для захисту трафіку. Потрібне налаштування **SNI** (Server Name Indication) для хостингу багатьох HTTPS-сайтів на одній IP-адресі. |

### 2.3. Віддалене Керування та Деплой 💻

| Протокол/Служба          | Порт за замовчуванням | Призначення та Безпека                                                                                                                 |
| :----------------------- | :-------------------- | :------------------------------------------------------------------------------------------------------------------------------------- |
| **RDP (Remote Desktop)** | **TCP 3389**          | Віддалене управління серверами (адміністрування). **Порада:** Змінюйте порт за замовчуванням та обмежуйте доступ через брандмауер/VPN. |
| **FTPS**                 | TCP 21 (control)      | Протокол передачі файлів **через TLS/SSL**. Використовуйте **явний FTPS** (Explicit FTPS) замість застарілого звичайного FTP.          |
| **Windows Admin Center** | HTTPS 443             | Сучасний веб-інструмент для адміністрування Windows Server (замість старого MMC).                                                      |

***

## 3. Мережеві Служби (Networking Essentials) 📡

* **DNS (Domain Name System):** Обов'язковий для AD. Перетворює доменні імена (наприклад, `server.corp.local`) на IP-адреси.
    * **Для розробника:** Проблеми з DNS-роздільною здатністю можуть викликати помилки підключення до SQL Server або API-сервісів.
* **DHCP (Dynamic Host Configuration Protocol):** Автоматична видача IP-адрес пристроям у мережі.
* **Firewall (Брандмауер):** Контролює вхідний та вихідний трафік. **Ключовий елемент безпеки.**
    * **Практика:** Для кожного застосунку чи порту (наприклад, 80, 443, 3389) має бути **спеціальне правило** у брандмауері.

***

## 4. Практичні Поради з Безпеки та Обслуговування 🛡️

1.  **Принцип Найменших Привілеїв (PoLP):** **Ніколи** не запускайте Application Pools або Service Accounts з правами **адміністратора** або `LocalSystem`. Використовуйте спеціалізовані **Service Accounts (Managed Service Accounts)**, які мають лише необхідні права.
2.  **Заміна FTP:** Повністю відмовтеся від **FTP** (незахищений текст). Завжди використовуйте **FTPS (FTP over TLS)** або **SFTP (SSH File Transfer Protocol)**.
3.  **Використовуйте Azure AD/Entra ID:** Якщо це можливо, переходьте на сучасні хмарні служби для управління ідентифікацією (**Azure AD/Entra ID**) замість локального AD DS.
4.  **Резервування/Snapshotting:** Робіть **знімки (snapshots)** віртуальної машини (VM) перед встановленням критичних оновлень, сертифікатів або значним деплоєм.
5.  **Hardening (Захист):** Регулярно перевіряйте конфігурацію безпеки (**TLS-версії, дозволені Cipher Suites**) на IIS та SQL Server. Використовуйте **IIS Crypto** або аналогічні інструменти для автоматизації цих налаштувань.