## 1. Основна Ідея та Роль 💡

**TCP (Transmission Control Protocol)** — це ключовий протокол **транспортного рівня** в моделі TCP/IP, який забезпечує **надійну, впорядковану та контрольовану** передачу даних між двома кінцевими точками (клієнтом і сервером).

> 🎯 **Принцип:** TCP гарантує, що дані дійдуть до адресата **саме в тому порядку**, в якому були відправлені, і **без втрат**. Це надійний, але повільніший механізм порівняно з UDP.

---

## 2. Механізми Надійності та Роботи ⚙️

TCP є **протоколом із встановленням з'єднання (Connection-Oriented)**.

### 2.1. Встановлення З'єднання (Three-way Handshake) 🤝

Перед початком обміну даними, клієнт і сервер обмінюються трьома сегментами для узгодження параметрів сесії:

1.  **SYN (Synchronize Sequence Number):** Клієнт надсилає запит на встановлення з'єднання.
2.  **SYN-ACK:** Сервер підтверджує запит клієнта (`ACK`) і надсилає свій власний запит на синхронізацію (`SYN`).
3.  **ACK (Acknowledge):** Клієнт підтверджує отримання `SYN-ACK`.

> **Результат:** З'єднання встановлено. Обидві сторони узгодили **порядкові номери (Sequence Numbers)** для подальшого обміну.

### 2.2. Гарантія Доставки та Порядок

| Механізм                                         | Призначення                                                                                                                                                                 |
| :----------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Порядкові Номери (Sequence Numbers)**          | Кожен сегмент даних має номер. Це дозволяє приймальній стороні **збирати сегменти в правильному порядку** (навіть якщо вони надійшли невпорядковано) та виявляти пропущені. |
| **Підтвердження (ACK)**                          | Приймальна сторона надсилає підтвердження (ACK) про успішне отримання сегмента.                                                                                             |
| **Таймаут і Повторна Передача**                  | Якщо відправник не отримує ACK протягом певного часу (таймауту), він вважає пакет втраченим і **повторно надсилає** його.                                                   |
| **Контроль Потоку (Flow Control)**               | Запобігає переповненню буфера приймальної сторони. Використовує механізм **Sliding Window** (ковзне вікно).                                                                 |
| **Контроль Перевантаження (Congestion Control)** | Регулює швидкість передачі, щоб уникнути перевантаження в самій мережі (маршрутизаторах, комутаторах).                                                                      |

---

## 3. Сценарії Використання у Microsoft-Екосистемі 🖥️

Практично всі критично важливі служби Microsoft використовують TCP, оскільки їм необхідна гарантія доставки:

| Служба                   | Порт (Стандарт) | Призначення TCP                                                           |
| :----------------------- | :-------------- | :------------------------------------------------------------------------ |
| **SQL Server (MSSQL)**   | **TCP 1433**    | Надійна передача запитів `SELECT/INSERT` та транзакцій.                   |
| **IIS / HTTPS**          | **TCP 443**     | Передача веб-запитів HTTP, надійно захищених **TLS/SSL**.                 |
| **RDP (Remote Desktop)** | **TCP 3389**    | Надійна передача команд керування сесією, що має пріоритет над швидкістю. |
| **SMTP / POP3 / IMAP**   | Різні порти     | Надійна доставка електронних листів.                                      |

---

## 4. Безпека та Практичні Поради для Розробника 🛡️

1.  **SYN Flood Attack:** Зловмисник може надсилати велику кількість `SYN`-запитів, але не надсилати фінальний `ACK`, переповнюючи буфер сервера (стадія `SYN_RECEIVED`).
    * **Захист:** Сучасні Firewall (Windows Firewall, Azure Firewall) мають вбудовані механізми захисту від SYN-флуду.
2.  **Протокол TLS/SSL:** Завжди використовуйте **TLS поверх TCP** (наприклад, HTTPS на 443 або FTPS) для захисту даних. TCP забезпечує *надійність*, а TLS — *конфіденційність*.
3.  **Firewall Hardening:** Використовуйте принцип **"deny all, allow only necessary"** (заборонити все, дозволити лише необхідне). Відкривайте порти лише для тих служб, які потрібні для роботи.
4.  **Моніторинг:** Для діагностики проблем із продуктивністю мережі використовуйте інструменти, які показують стан TCP-з'єднань:
    * **`netstat`** (стани `ESTABLISHED`, `TIME_WAIT`, `LISTEN`).
    * **Wireshark / Network Monitor** для захоплення та аналізу мережевого трафіку.