## 1. Основна Ідея та Роль 💡

**RDP (Remote Desktop Protocol)** — це пропрієтарний протокол Microsoft, який дозволяє користувачу графічно взаємодіяти з робочим столом іншого комп'ютера або сервера через мережу, створюючи ілюзію локальної роботи.

### 🎯 Ключова Роль:

* **Адміністрування (Server Administration):** Основний спосіб управління Windows Server, SQL Server та IIS.
* **Віддалена Робота (Remote Work):** Забезпечення доступу користувачів до корпоративних ресурсів.

---

## 2. Технічна Реалізація та Транспорт 📡

| Термін | Значення | Важливість |
| :--- | :--- | :--- |
| **Порт за замовчуванням** | **TCP 3389** | **Критично:** Часто є об'єктом сканування та атак. Рекомендується зміна або обмеження доступу через Firewall. |
| **Мультисесійність** | Можливість одночасної роботи декількох користувачів на одному сервері. | Використовується для **Remote Desktop Services (RDS)**, що є основою VDI (Virtual Desktop Infrastructure). |
| **Протокол Transport** | **TCP** (для надійності) та **UDP** (для графічного прискорення). | **UDP** використовується для кращої продуктивності при передачі відеопотоку та графіки (через **RemoteFX**). |
| **RemoteFX** | Набір технологій (починаючи з Windows Server 2008 R2), що покращує передачу графіки та мультимедіа в RDP-сесіях. | Забезпечує якісний досвід роботи навіть із 3D-графікою. |

---

## 3. Безпека та Налаштування Доступу (Hardening) 🛡️

Найбільші ризики безпеки пов'язані з відкритим портом RDP у світ. Необхідно налаштовувати захист на кількох рівнях:

### 3.1. Аутентифікація та Шифрування

| Механізм | Опис | Чому це критично? |
| :--- | :--- | :--- |
| **NLA (Network Level Authentication)** | Вимагає **автентифікації користувача** (введення логіна/пароля) ще **до** встановлення повноцінної RDP-сесії. | Захищає від DDoS-атак та зменшує навантаження на ресурси сервера від невдалих спроб входу. **Завжди вмикати.** |
| **TLS/SSL** | Протокол RDP використовує TLS (зазвичай TLS 1.2+) для шифрування **всього трафіку сесії**. | Забезпечує **конфіденційність** даних, що передаються (натискання клавіш, вміст екрана). |
| **Двофакторна автентифікація (2FA)** | Налаштування додаткового рівня перевірки (через Azure MFA, RADIUS або сторонні рішення). | **Найкраща практика:** Запобігає компрометації сесії, навіть якщо пароль було викрадено. |

### 3.2. Практичні Політики та Обмеження

1.  **Зміна Порту:** Зміна стандартного порту **3389** на нестандартний (наприклад, 44000) для уникнення автоматичного сканування ботами.
2.  **IP-Обмеження (Firewall):** Обмеження доступу до порту RDP лише для **певних статичних IP-адрес** або **корпоративної VPN-мережі**.
3.  **Групові Політики (GPO):** Використання GPO для визначення, які **AD-групи** мають право входити через RDP (права `Remote Desktop Users`).
4.  **Лімітування Сесій:** Налаштування автоматичного виходу або відключення неактивних сесій (таймаути) для звільнення ресурсів сервера.

---

## 4. Коротке Резюме 🚀

**RDP** — незамінний інструмент для управління серверами. Безпека критично залежить від **правильної конфігурації**, де **NLA** та **Firewall-обмеження** є мінімальними вимогами. Використання **TLS/SSL** гарантує конфіденційність трафіку.