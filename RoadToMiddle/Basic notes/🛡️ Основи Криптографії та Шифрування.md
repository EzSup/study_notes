## 1. Концептуальна Основа: Навіщо Шифрування?

**Шифрування (Encryption)** — це процес перетворення вихідних даних (відкритий текст, **plaintext**) у незрозумілу, нечитабельну форму (шифротекст, **ciphertext**) за допомогою **алгоритму** та **ключа**.

У Microsoft-середовищі (Windows, SQL Server, Azure, .NET) шифрування забезпечує три ключові аспекти безпеки:

1.  **Конфіденційність (Confidentiality):** Запобігає несанкціонованому доступу до даних.
2.  **Цілісність (Integrity):** Гарантує, що дані не були змінені. (Зазвичай досягається **хешуванням**).
3.  **Автентифікація (Authentication):** Підтверджує особу відправника або справжність даних.

***

## 2. Ключові Види Криптографії та Алгоритми

### 2.1. Симетричне Шифрування (Symmetric Cryptography) 🔑

* **Принцип:** Використовується **один і той самий секретний ключ** як для шифрування, так і для розшифрування.
* **Використання:** Завдяки високій швидкості і ефективності, використовується для шифрування **великих обсягів даних** (наприклад, файлів, мережевого трафіку).
* **Головний Алгоритм:** **AES (Advanced Encryption Standard)**.
    * **Для .NET-Розробника:** Реалізовано у класах `System.Security.Cryptography.Aes` (у .NET Core/5+) та `AesCryptoServiceProvider`.
    * **Рекомендовані Розміри Ключа:** **AES-256** є галузевим стандартом.
* **Додаткові Параметри (Важливо для .NET):**
    * **IV (Initialization Vector):** Унікальне випадкове значення, яке **НЕ** є секретним і використовується для ініціалізації блочного шифру. Необхідно для уникнення однакового шифротексту при шифруванні однакового відкритого тексту. Його потрібно передавати разом із шифротекстом.
    * **Режими Шифрування (e.g., CBC, GCM):** Визначають, як блочний шифр (наприклад, AES) застосовується до блоків даних. **GCM (Galois/Counter Mode)** є сучасним і забезпечує *автентифіковане шифрування* (цілісність та конфіденційність одночасно).

### 2.2. Асиметричне Шифрування (Asymmetric Cryptography / Public-Key) 🔐

* **Принцип:** Використовується **пара ключів**:
    1.  **Публічний Ключ (Public Key):** Доступний усім; використовується для **шифрування** або **перевірки цифрового підпису**.
    2.  **Приватний Ключ (Private Key):** Секретний; використовується для **розшифрування** або **створення цифрового підпису**.
* **Використання:** Завдяки низькій швидкості, використовується лише для **обміну симетричними ключами** та **цифрових підписів**.
* **Головний Алгоритм:** **RSA** (також ECC/Еліптичні криві — більш сучасний).
    * **Для .NET-Розробника:** Класи `System.Security.Cryptography.RSA` та `RSACryptoServiceProvider`.
    * **Рекомендовані Розміри Ключа:** **RSA 2048+** біт.
* **Сценарій:** Відправник шифрує симетричний ключ публічним ключем отримувача. Отримувач розшифровує його своїм приватним ключем.

### 2.3. Хешування та HMAC (Integrity & Authenticity) 📝

* **Хешування (Hashing):** Односторонній математичний процес, що створює фіксований рядок (хеш/дайджест) з даних. Незворотне.
    * **Мета:** **Перевірка цілісності** (якщо дані змінені, хеш буде іншим).
    * **Алгоритми:** SHA-256, SHA-512 (SHA-1 та MD5 вважаються скомпрометованими).
* **HMAC (Hash-based Message Authentication Code):** Різновид хешування, що використовує **секретний ключ** для генерації хешу.
    * **Мета:** **Перевірка автентичності та цілісності.** Тільки той, хто знає секретний ключ, може створити або перевірити HMAC.
    * **Для .NET-Розробника:** Клас `System.Security.Cryptography.HMACSHA256`.

***

## 3. Типові Сценарії у Microsoft-Екосистемі (.NET, SQL, Azure)

| Сценарій | Роль Криптографії | Де використовується у .NET/SQL |
| :--- | :--- | :--- |
| **HTTPS/TLS** | **HOLAP (Hybrid OLAP)**: Асиметричне шифрування (RSA/ECC) для **обміну симетричними ключами**; Симетричне шифрування (AES) для **передачі даних**. | Вбудовано у `HttpClient`, ASP.NET Core, `SslStream`. |
| **SQL Server TDE** | **Симетричне (AES)**: Шифрування **всього файлу бази даних** на дисковому рівні (**Transparent Data Encryption**). | Прозоро для .NET-коду; управляється на рівні SQL Server. |
| **Сертифікати (Certificates)** | **Асиметричне (RSA)**: Використовуються для **автентифікації, підпису** та захисту приватного ключа. | `System.Security.Cryptography.X509Certificates`. Керування ключами у **Windows Certificate Store**. |
| **RDP (Remote Desktop)** | **Симетричне та Асиметричне (TLS):** Забезпечує конфіденційність сесії. | Використовує криптографічні бібліотеки Windows. |
| **ASP.NET Core Data Protection** | **Симетричне (AES) та HMAC:** Автоматично використовується для захисту Cookies, токенів та ін. | Вбудовано у фреймворк; керується класом `IDataProtectionProvider`. |

***

## 4. Безпека та Практичні Поради для .NET Developer 🛠️

1.  **Уникайте WCF Data Contract Serializer та Rijndael:** У старих .NET Framework для шифрування часто використовували клас `RijndaelManaged` (старий стандарт), тоді як сучасний `Aes` є пріоритетним. Уникайте старих, скомпрометованих алгоритмів.
2.  **Завжди використовуйте GCM/HMAC:** Забезпечуйте **автентифіковане шифрування**. Недостатньо просто зашифрувати дані (конфіденційність); потрібно перевірити їхню цілісність (цілісність). Використовуйте режими, що включають HMAC (наприклад, AES-GCM) або додавайте HMAC (наприклад, `HMACSHA256`) вручну.
3.  **Захист Ключів:** Ніколи не зберігайте секретні ключі та паролі у відкритому вигляді у коді (`appsettings.json`, тощо).
    * **Рекомендації:** **Azure Key Vault**, **AWS KMS** або **HashiCorp Vault** для зберігання секретів.
    * **DPAPI (Data Protection API):** Для локального захисту секретів у Windows/ASP.NET.
4.  **Управління Сертифікатами:** При роботі з асиметричним шифруванням, керуйте життєвим циклом сертифікатів (RSA 2048+). Сертифікати мають властивість закінчуватися.
5.  **Випадковість:** Завжди використовуйте криптографічно стійкий генератор випадкових чисел (CSPRNG), наприклад, `System.Security.Cryptography.RandomNumberGenerator`, для генерації ключів та IV. **НЕ** використовуйте `System.Random`.

***

## 5. Коротке Резюме 🚀

* **Швидкість:** **Симетричне шифрування (AES-256)** є швидким і використовується для шифрування основного потоку даних.
* **Обмін:** **Асиметричне шифрування (RSA 2048+)** є повільним і використовується для безпечного обміну симетричними ключами та цифрового підпису (TLS/Сертифікати).
* **Цілісність:** **Хешування (SHA-256) та HMAC** використовуються для гарантування того, що дані не були змінені.
* **Для Middle Developer:** Основний фокус на **правильному управлінні ключами (IV, секрети)** та забезпеченні **автентифікованого шифрування**.