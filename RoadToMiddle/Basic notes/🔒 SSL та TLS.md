## 1. Концепція та Термінологія 💡

| Термін                             | Значення                                                             | Примітка                                                                    |
| :--------------------------------- | :------------------------------------------------------------------- | :-------------------------------------------------------------------------- |
| **SSL** (Secure Sockets Layer)     | Початковий протокол.                                                 | **Вважається застарілим і скомпрометованим.** Не повинен використовуватися. |
| **TLS** (Transport Layer Security) | Сучасний протокол.                                                   | Фактичний стандарт безпеки даних **поверх TCP** (наприклад, порт 443).      |
| **HTTPS**                          | HTTP + TLS/SSL.                                                      | Протокол захищеного веб-трафіку.                                            |
| **Сертифікат**                     | Файл (.cer, .pfx) з **публічним ключем** та даними про особу/сервер. | Використовується для **автентифікації** сервера клієнту.                    |
| **CA** (Certificate Authority)     | Довірений центр, який видає сертифікати.                             | Підтверджує справжність сертифіката сервера.                                |

***

## 2. Як Працює TLS-Рукостискання (Handshake) 🤝

TLS-рукостискання — це асиметричний обмін, який встановлює безпечний симетричний канал.

| Крок | Дія | Забезпечуваний Аспект |
| :--- | :--- | :--- |
| **1. Client Hello** | Клієнт надсилає серверу список підтримуваних **Cipher Suites** (алгоритмів шифрування) та бажану версію **TLS** (наприклад, 1.3). | Початок комунікації, узгодження можливостей. |
| **2. Server Hello** | Сервер обирає найкращий спільний алгоритм, надсилає свій **сертифікат** (з публічним ключем) та підтверджує обрану версію TLS. | Автентифікація сервера. |
| **3. Certificate Verify** | Клієнт перевіряє сертифікат: чи він дійсний, чи виданий довіреним **CA**, і чи не закінчився термін дії. | Довіра до сервера. |
| **4. Key Exchange** | Клієнт генерує унікальний **симетричний ключ сесії (Session Key)** та шифрує його **публічним ключем** сервера. | Забезпечення конфіденційності ключа. |
| **5. Finished** | Сервер розшифровує ключ своїм **приватним ключем**. Відтепер обидві сторони мають **спільний симетричний ключ**. | Встановлення симетричного каналу. |

> **Резюме шифрування:** **Асиметричне (RSA)** використовується лише для **обміну ключами**. **Симетричне (AES)** використовується для **всієї подальшої передачі даних** (через високу швидкість).

***

## 3. Сценарії Використання у Microsoft-Екосистемі 🌐

| Сервіс/Технологія | Мета TLS | .NET/SQL Конфігурація |
| :--- | :--- | :--- |
| **ASP.NET Core / IIS** | **HTTPS** для веб-застосунків. | Сертифікат прив'язується до порту 443 у IIS або Kestrel. |
| **SQL Server** | Шифрування **клієнтських з'єднань** (TDS-протоколу). | Встановлення прапора `Encrypt=True` у рядку підключення; Серверу потрібен сертифікат. |
| **Azure Services** | Всі з'єднання (Web Apps, Storage, Key Vault). | Примусове використання TLS 1.2+ за замовчуванням. |
| **RDP / FTPS** | Захист сеансу віддаленого робочого столу / передачі файлів. | Вимагає сертифіката на сервері для TLS. |

***

## 4. Безпека та Рекомендації для Розробника 🛡️

| Рекомендація | Чому це важливо? |
| :--- | :--- |
| **TLS 1.2 / TLS 1.3** | **TLS 1.3** є поточним стандартом. Завжди **вимкніть TLS 1.0, 1.1** і всі версії SSL на сервері. |
| **Private Key Security** | **Приватний ключ** сертифіката є найціннішим активом. Його компрометація дозволяє зловмиснику дешифрувати трафік. |
| **Cipher Suites Hardening** | Обмежте набір дозволених алгоритмів лише сучасними (наприклад, AES-256-GCM, ECDHE). Вимкніть слабкі шифри. |
| **Certificate Pinning** | Вбудовуйте інформацію про очікуваний сертифікат у клієнтський .NET-додаток, щоб запобігти MITM-атакам (Man-In-The-Middle), навіть якщо CA скомпрометований. |
| **Expiration Check** | **Автоматизуйте** перевірку терміну дії сертифікатів (моніторинг/Azure Key Vault). Недотримання цього призводить до простою системи. |